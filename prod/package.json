{
  "name": "screeps-bot-prod",
  "version": "0.1.0",
  "private": true,
  "description": "Screeps: World bot production source",
  "main": "dist/main.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest --runInBand",
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --platform=node --format=cjs --target=es2019 --outfile=dist/main.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/screeps": "^3.3.8",
    "esbuild": "^0.28.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
